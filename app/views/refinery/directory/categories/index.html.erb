<% content_for :body_content_left do %>
  <ul id="categories">
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.title, refinery.directory_category_path(category) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
